# Frontend UI & Styling

## UI & Visuals
- Utilize `lucide-react` for iconography.
- Utilize `recharts` for data visualization. Ensure chart data is properly formatted to match Recharts' expected input structures. (Note: Data memoization rules reside in the performance guidelines).
